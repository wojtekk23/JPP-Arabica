int f(int x) {
    print(x);
    return f(x-1);
}

int main() {
    int x = 5;
    int y = x + 2;
    print(y);

    while (y > 0) {
        y = y - 1;
    }

    x = f(x);

    print( x );
    print( y );
}
