int f(int x) {
    if (x == 0)
        return 1;
    return x*f(x-1);
}

int main() {
    int x = 5;
    int y = x + 2;
    print(y);

    while (y > 0) {
        y = y - 1;
    }

    x = f(5);

    print( x );
    print( y );

    return 0;
}
